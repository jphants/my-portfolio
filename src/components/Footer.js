const Footer = () => {
    return (
        <footer style={{ background: "#333", color: "#fff", padding: "20px", textAlign: "center" }}>
            <div>
                Footer content
            </div>
        </footer>
    );
}

export default Footer;
